# ── FIX-02/03/04/05/06 ───────────────────────────────────────────────
# Pin Streamlit >= 1.35.0 to guarantee:
#   st.segmented_control (added 1.29)
#   st.pills             (added 1.32)
#   st.audio_input       (added 1.33)
#   st.status            (added 1.28)
#   st.popover           (added 1.31)
# ─────────────────────────────────────────────────────────────────────
streamlit>=1.35.0
 
# Core AI APIs
groq>=0.9.0
google-generativeai>=0.8.0
 
# Audio processing
# audioop-lts bridges Python 3.13+ removal of the audioop stdlib module
# Must come BEFORE pydub so pydub finds it at import time
audioop-lts>=0.2.1
pydub>=0.25.1
 
# PDF generation — fpdf2 (not fpdf1)
# ── FIX-14 ───────────────────────────────────────────────────────────
# fpdf2 >= 2.7 deprecates ln=True in cell().
# All ln=True calls replaced with new_x/new_y in the codebase.
# Pin >= 2.7.4 which is stable on Streamlit Cloud.
# ─────────────────────────────────────────────────────────────────────
fpdf2>=2.7.4
